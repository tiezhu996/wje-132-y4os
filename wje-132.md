<!-- 改编自：建筑仓储车辆主题项目提示词/wje-132/wje-132.md；后端由 NestJS + TypeScript 改造为 Go 1.22 + Gin + GORM -->
# wje-132 建筑施工安全管理平台

请根据以下要求生成项目代码和 README。

## 项目主要需求

### 1. 项目概述

**建筑施工安全管理平台（SafetyPlatform）** 面向工地安全管理员和监理人员，支持安全隐患排查、事故记录与跟踪、安全培训管理和人员资质证书管理。项目类型：**全栈 Web 应用**。

### 2. 核心实体（≥ 3 个，必须贯穿全栈）

- **User（用户）**
  - 字段：id, phone, password_hash, name, avatar, role（Admin/SafetyManager/Inspector/Worker）, created_at
  - 关键关系：用户可上报多个 SafetyIncident、执行多个 SafetyInspection、参与多个 SafetyTraining、持有多个 WorkerCertification
  - 业务动作：注册/登录、修改资料、上传头像
  - **此实体必须贯穿：数据库表 → backend/internal/model/user.go → backend/internal/repository/user_repository.go → backend/internal/service/user_service.go → backend/internal/handler/user_handler.go → backend/internal/router/users.go → frontend/src/api/user.ts → frontend/src/stores/userStore.ts → frontend/src/pages/Profile.tsx**

- **SafetyIncident（安全事件）**
  - 字段：id, title, description, occurred_at, site_id, area, severity_level（NearMiss/Minor/Moderate/Major/Fatal）, category（坠落/触电/物体打击/坍塌/机械伤害/其他）, involved_user_ids（JSON）, photo_urls（JSON）, status（Reported/Investigating/Resolved/Closed）, rectification_measures, rectification_deadline, reporter_id
  - 关键关系：隶属于上报人（User.reporter），被多个审计日志引用
  - 业务动作：上报事件、指派调查、提交整改、关闭事件
  - **此实体必须贯穿全栈，禁止在单一文件中处理 CRUD**

- **SafetyInspection（安全检查）**
  - 字段：id, name, inspection_type（Routine/Special/PreShift/Emergency）, area, inspection_date, inspector_id, total_score（0-100）, status（Scheduled/InProgress/Completed/Failed）, issue_count, passed_count
  - 关键关系：隶属于检查人（User.inspector），包含多个 InspectionItem
  - 业务动作：创建检查计划、执行检查、记录结果、生成报告
  - **此实体必须贯穿全栈**

- **InspectionItem（检查项）**
  - 字段：id, inspection_id, item_name, passed, remark, photo_url
  - 关键关系：隶属于 SafetyInspection
  - 业务动作：执行检查时逐项勾选是否合格并拍照
  - **此实体必须贯穿全栈**

- **SafetyTraining（安全培训）**
  - 字段：id, topic, training_type（Induction/Regular/Special/Emergency）, training_date, duration_hours, trainer, location, content_summary, participant_ids（JSON）, assessment_method（笔试/实操/口试）, pass_rate
  - 关键关系：可关联多个 User（参训人员）
  - 业务动作：创建培训、记录签到、录入成绩、生成培训记录
  - **此实体必须贯穿全栈**

- **WorkerCertification（人员资质）**
  - 字段：id, user_id, cert_type（特种作业证/安全员证/电工证/焊工证/高空作业证等）, cert_no, issue_org, issue_date, valid_until, cert_photo_url, status（Pending/Approved/Rejected/Expired）
  - 关键关系：隶属于 User
  - 业务动作：提交资质、审核资质、到期提醒、续期管理
  - **此实体必须贯穿全栈**

### 3. 核心页面（≥ 4 个，至少 2 个页面共用同一业务组件）

- **/dashboard（安全概览仪表盘）**
  - 消费实体：SafetyIncident、SafetyInspection、SafetyTraining
  - 关键交互：近 30 天事件趋势折线图、风险等级分布饼图、待整改项列表、本月培训完成率
  - 共享组件：`<StatusBadge>`、`<RiskLevelTag>`

- **/incidents（事件管理）**
  - 消费实体：SafetyIncident、User
  - 关键交互：事件列表（按严重等级、状态、时间筛选）、新增事件表单（含图片上传）、事件详情（时间线展示调查和整改过程）
  - 共享组件：`<StatusBadge>`（与 /dashboard 共用）、`<RiskLevelTag>`

- **/inspections（检查管理）**
  - 消费实体：SafetyInspection、InspectionItem
  - 关键交互：检查计划列表、执行检查（逐项勾选是否合格 + 拍照）、检查结果报告页
  - 共享组件：`<StatusBadge>`、`<EmptyState>`

- **/trainings（培训管理）**
  - 消费实体：SafetyTraining、User
  - 关键交互：培训日历视图、培训详情（签到表 + 成绩表）、培训记录导出
  - 共享组件：`<StatusBadge>`、`<UserAvatar>`

- **/certifications（资质审核）**
  - 消费实体：WorkerCertification、User
  - 关键交互：待审核列表、资质详情（含证书照片预览）、过期预警列表
  - 共享组件：`<StatusBadge>`（与 /incidents 共用）、`<RiskLevelTag>`

- **/profile（个人中心）**
  - 消费实体：User
  - 关键交互：修改资料、查看我的上报与资质
  - 共享组件：`<AvatarUploader>`、`<EmptyState>`

### 4. 横切关注点（≥ 2 个）

- **认证授权（JWT + RBAC）**
  - 触达文件层：
    - 数据库：`users` 表含 role 字段
    - 后端：`internal/middleware/auth.go`、`internal/middleware/rbac.go`、`internal/util/jwt.go`
    - 路由：`backend/internal/router/*.go` 中资质审核、事件指派等接口需 `AuthRequired()` 和 `RequireRole()`（Worker 只能上报事件不能审核）
    - 前端：`router/guards.tsx`、`components/common/RoleGuard.tsx`、`stores/authStore.ts`

- **审计日志**
  - 触达文件层：
    - 数据库：`audit_logs` 表
    - 后端：`internal/middleware/audit_log.go`、`internal/service/*.go` 埋点、`internal/constants/log_templates.go`
    - 前端：`pages/AuditLogs.tsx`、`types/auditLog.ts`

- **全局错误处理**
  - 触达文件层：
    - 后端：`internal/middleware/error_handler.go`、`internal/util/app_error.go`、`internal/constants/error_codes.go`
    - 前端：`utils/request.ts` 拦截器、`components/common/ErrorBoundary.tsx`

### 5. 共享枚举/常量（≥ 2 组）

- **SeverityLevel（严重等级）**
  - 值：NEAR_MISS = 'near_miss', MINOR = 'minor', MODERATE = 'moderate', MAJOR = 'major', FATAL = 'fatal'
  - 前端定义位置：`frontend/src/constants/incident.ts`
  - 后端定义位置：`backend/internal/constants/incident.go`
  - 出现位置：SafetyIncident 模型、IncidentService、RiskLevelTag、事件列表/详情、日志模板、错误码
  - **README 必须列出该枚举在前后端的所有出现位置**

- **IncidentStatus（事件状态）**
  - 值：REPORTED = 'reported', INVESTIGATING = 'investigating', RESOLVED = 'resolved', CLOSED = 'closed'
  - 前端定义位置：`frontend/src/constants/incident.ts`
  - 后端定义位置：`backend/internal/constants/incident.go`
  - 出现位置：SafetyIncident 模型、IncidentService 状态机、StatusBadge、事件列表/详情、日志模板、错误码
  - **README 必须列出该枚举在前后端的所有出现位置**

- **UserRole（用户角色）**
  - 值：ADMIN = 'admin', SAFETY_MANAGER = 'safety_manager', INSPECTOR = 'inspector', WORKER = 'worker'
  - 前端定义位置：`frontend/src/constants/user.ts`
  - 后端定义位置：`backend/internal/constants/user.go`
  - 出现位置：User 模型、RBAC 中间件、路由权限、前端按钮显隐、路由守卫
  - **README 必须列出该枚举在前后端的所有出现位置**

### 6. 共享前端组件清单（≥ 3 个）

- `<StatusBadge>`：被 /dashboard、/incidents、/inspections、/trainings、/certifications 共用
- `<RiskLevelTag>`：被 /dashboard、/incidents、/certifications 共用（按风险等级着色）
- `<UserAvatar>`：被 /incidents、/trainings、/certifications 共用
- `<EmptyState>`：被 /inspections、/profile 等多个列表页共用
- `<AvatarUploader>`：被 /profile 使用

### 7. 共享 hooks/utils 清单（≥ 2 个）

- `useIncident()`：封装事件详情获取与状态流转逻辑
- `usePagination()`：通用分页逻辑
- `utils/getSeverityColor.ts`：根据严重等级返回对应颜色常量
- `utils/dateFormat.ts`：日期时间格式化

### 8. 后端中间件清单（≥ 2 个）

- `auth.go`：验证 JWT，将用户信息注入 gin.Context（`c.Set("user", ...)`）
- `rbac.go`：基于用户角色校验权限
- `audit_log.go`：审计日志记录
- `error_handler.go`：统一错误响应格式
- `rate_limiter.go`：接口限流
- `upload.go`：图片上传处理（事件现场照片、证书照片）

### 9. 技术要求

- **英文短名（用于 Docker Compose）**：`safety-platform`
- **前端端口**：18702
- **后端端口**：19202
- **前端框架**：React 18 + TypeScript
- **UI 库**：Ant Design 5
- **图表库**：ECharts
- **状态管理**：Zustand
- **构建工具**：Vite
- **后端框架**：Go 1.22 + Gin + GORM
- **数据库**：MySQL 8.0
- **ORM**：GORM
- **认证方式**：JWT + RBAC（使用 `github.com/golang-jwt/jwt/v5`）
- **其他依赖**：`github.com/gin-gonic/gin`、`gorm.io/gorm`、`gorm.io/driver/mysql`、`github.com/gin-contrib/cors`、`golang.org/x/crypto/bcrypt`、`github.com/go-playground/validator/v10`

### 10. 文件结构强制清单

前端必须包含：
```
frontend/src/
├── api/              # user.ts, incident.ts, inspection.ts, training.ts, certification.ts
├── stores/           # authStore.ts, userStore.ts, incidentStore.ts, inspectionStore.ts, trainingStore.ts
├── types/            # 共享类型定义
├── components/common/# StatusBadge, RiskLevelTag, UserAvatar, EmptyState, AvatarUploader, RoleGuard, ErrorBoundary
├── hooks/            # useIncident.ts, usePagination.ts
├── pages/            # Dashboard, IncidentManage, InspectionManage, TrainingManage, CertReview, Profile, AuditLogs
├── router/           # index.tsx + guards.tsx
├── utils/            # getSeverityColor.ts, dateFormat.ts, request.ts
└── constants/        # incident.ts, user.ts, errorCodes.ts
```

后端必须包含（Go 标准布局）：
```
backend/
├── go.mod
├── go.sum
├── cmd/
│   └── server/
│       └── main.go              # 程序入口：加载配置、初始化 DB、装配依赖并启动 Gin
└── internal/
    ├── config/                  # config.go（数据库连接、JWT、限流、上传、端口等）
    ├── model/                   # 按实体分文件：user.go, safety_incident.go, safety_inspection.go, inspection_item.go, safety_training.go, worker_certification.go
    ├── repository/              # 按实体分文件：user_repository.go, safety_incident_repository.go, safety_inspection_repository.go, inspection_item_repository.go, safety_training_repository.go, worker_certification_repository.go
    ├── service/                 # 按实体分文件：user_service.go, safety_incident_service.go, safety_inspection_service.go, inspection_item_service.go, safety_training_service.go, worker_certification_service.go
    ├── handler/                 # 按实体分文件：user_handler.go, safety_incident_handler.go, safety_inspection_handler.go, inspection_item_handler.go, safety_training_handler.go, worker_certification_handler.go
    ├── router/                  # router.go + 按实体分文件：users.go, safety_incidents.go, safety_inspections.go, inspection_items.go, safety_trainings.go, worker_certifications.go
    ├── middleware/              # auth.go, rbac.go, audit_log.go, error_handler.go, rate_limiter.go, upload.go
    ├── dto/                     # 请求/响应结构体
    ├── constants/               # incident.go, user.go, error_codes.go, log_templates.go, messages.go
    └── util/                    # jwt.go, logger.go, formatters.go, app_error.go, file_upload.go
```

**严禁合并职责到单一文件。即使是简单功能，也必须按照上述分层结构拆分，确保后续每一个小改动都至少触达 3-5 个文件。**

### 11. 特殊约束：屎山代码设计（低内聚、高耦合、牵一发动全身）

为了验证模型的跨文件协同改动能力，本项目在规范分层的基础上，额外要求刻意制造合理的"屎山"耦合：

1. **日志模块必须单独管理但全栈引用**
   - 后端必须有一个 `internal/util/logger.go`，使用 `log/slog` 或 `zap` 封装
   - 所有 handler、service、middleware 都必须引用 logger
   - 日志格式字符串必须集中定义在 `internal/constants/log_templates.go`，包含至少 25 条模板，如 `INCIDENT_CREATE_SUCCESS`、`INCIDENT_CLOSE_FAILED`
   - 任何业务字段变更（如 SafetyIncident 新增字段）都必须同步修改对应的日志模板和调用处

2. **异常信息必须分散且层层透传**
   - 错误码集中在 `internal/constants/error_codes.go`，但每个 service/handler 都要手动拼接 message
   - 禁止在单一地方统一处理所有异常，必须在 handler 中再次包装 service 返回的错误
   - 错误 message 中必须包含实体名、字段名、角色名，如 `"SafetyIncident[id=123] close failed: worker not allowed"`

3. **常量/工具类必须多处耦合**
   - `internal/util/formatters.go` 中同时包含日期格式化、严重等级文本、事件状态文本、角色文本、检查类型文本
   - 多个 handler 和 service 都直接引用 formatters，而不是独立拆分
   - `internal/constants/messages.go` 同时包含前端提示文案、后端返回文案、日志文案
   - 修改一个文案可能同时影响前端显示、后端返回和日志内容

4. **事件状态机必须跨多处定义**
   - IncidentStatus 的流转规则在 service 状态机、前端按钮显隐、日志模板、错误码、formatters 中同时存在
   - 新增一个状态值需要修改至少 10 处

5. **枚举必须前后端重复定义且被多处引用**
   - SeverityLevel 和 IncidentStatus 必须在前端 constants、后端 constants、类型定义、日志模板、错误码、formatters、GORM 模型中同时存在
   - 新增一个枚举值时，必须修改至少 10 处文件

6. **牵一发动全身的验证标准**
   - 后续如果要求"给 SafetyIncident 新增一个 'chemical' 事件分类"，必须触达：GORM AutoMigrate、SafetyIncident 模型、前后端 constants、IncidentService、StatusBadge、RiskLevelTag、formatters、日志模板、错误码、Dashboard 风险分布统计、事件筛选器等 ≥ 10 个文件

## Go 企业级工程规范（必须遵循）

后端 Go 代码除满足上述功能与分层要求外，还必须遵循当前 Go 语言通用的企业级工程规范：

### 1. 项目目录规范
- 根目录 `go.mod` 声明 Go 版本 `go 1.22`，模块名使用公司/项目规范（如 `github.com/<org>/<project>`）。
- 采用 `cmd/` + `internal/` + `pkg/` 布局：`cmd/server/main.go` 只负责加载配置、装配依赖、启动服务；私有业务代码全部放在 `internal/`；仅无业务依赖的可复用库放 `pkg/`。
- 数据库迁移脚本放 `migrations/`，接口定义/OpenAPI 文档放 `api/`，Docker/K8s 部署文件放 `deploy/`。
- 包名使用小写单词、不加下划线；文件使用 `snake_case.go`；导出标识符用 `CamelCase`，未导出的用 `camelCase`；禁止使用拼音命名。

### 2. 分层与依赖方向
- 依赖方向严格单向：`handler → service → repository → model`，`model` 不依赖任何上层，禁止反向引用与包循环依赖。
- 每个核心实体按 `model_<entity>.go`、`repository_<entity>.go`、`service_<entity>.go`、`handler_<entity>.go` 分文件，职责单一。
- 依赖通过构造函数注入：`func NewXService(repo Repo, logger *slog.Logger) *XService`；禁止在业务代码中全局 `init` 连接数据库或散落 `sql.Open`。

### 3. 错误处理
- 使用 `fmt.Errorf("list users: %w", err)` 包裹并透传错误，保留错误链。
- 仓储层定义哨兵错误（如 `var ErrNotFound = errors.New("not found")`），上层用 `errors.Is` 判断。
- 禁止在 service 层打印日志后吞掉错误；错误统一由 `middleware` 或 `handler` 转换为标准 JSON 响应。
- 所有 error 必须被处理，关键错误禁止 `_ =` 忽略。

### 4. 配置与日志
- 配置通过环境变量注入，在 `internal/config/config.go` 集中解析，推荐使用 `github.com/caarlos0/env/v11` 或 `spf13/viper`。
- 日志使用 `log/slog` 或 `go.uber.org/zap` 结构化输出；请求日志统一包含 `request_id`、`method`、`path`、`status`、`latency_ms`。

### 5. 参数校验与响应
- 入参校验使用 `github.com/go-playground/validator/v10`，在 DTO 结构体 tag 中声明规则。
- 路由统一 `/api/v1` 前缀；健康检查为 `/healthz`；分页参数统一为 `page`、`page_size`。
- 响应统一为 `{ "code": 0, "message": "ok", "data": ... }` 包裹，错误码集中维护。

### 6. 测试与文档
- 单元测试使用标准库 `testing` + 表驱动（table-driven），文件命名 `*_test.go`；service 与 repository 必须有对应测试。
- 对外接口提供 OpenAPI/Swagger（如 `github.com/swaggo/gin-swagger`）或在 README 中列出完整 API 清单。

### 12. 部署要求

这是前后端分离项目。

前端代码放在 `frontend/`：
- 页面、组件、路由、状态管理、API 请求、类型定义、样式和资源等放在对应的子文件夹

后端代码放在 `backend/`：
- 数据库脚本放在 `database/`。

项目必须支持 Docker Compose 一键部署，且在 **任意目录名（包括中文目录名）** 下都能正常启动。

需要提供以下文件：
- 根目录 `docker-compose.yml`：编排前端、后端、数据库及所有依赖服务
- 根目录 `.env`：包含 `COMPOSE_PROJECT_NAME=safety-platform`
- `frontend/Dockerfile`：多阶段构建，先构建再用 Nginx 托管
- `backend/Dockerfile`：Go 多阶段构建（见下方骨架）
- `frontend/nginx.conf`：Nginx 配置，处理前端路由和 API 反向代理
- `.env.example`：所有环境变量的示例文件，附注释说明；同时包含 `COMPOSE_PROJECT_NAME` 默认值

backend/Dockerfile 使用以下 Go 多阶段构建骨架：
```
FROM golang:1.22-alpine AS builder
WORKDIR /app
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 GOOS=linux go build -o /app/server ./cmd/server
FROM alpine:3.20
RUN apk add --no-cache ca-certificates tzdata
WORKDIR /app
COPY --from=builder /app/server .
EXPOSE 8080
CMD ["/app/server"]
```

docker-compose.yml 要求：
- **不要写 `version:` 字段**
- 顶层声明 `name: safety-platform`
- 所有服务使用 `container_name` 命名容器，容器名带 `${COMPOSE_PROJECT_NAME:-safety-platform}` 前缀
- 数据库数据使用命名卷持久化（`volumes:` 顶层声明，不要使用绑定挂载到中文路径）
- 前端通过 Nginx 反向代理后端 API（前端代码里调用 `/api`，由 Nginx 转发到 `backend:8080`，不要硬编码 `localhost`）
- 服务间通过 Docker 内部网络通信，服务名即主机名
- 数据库服务配置 `healthcheck`，后端服务通过 `depends_on: { db: { condition: service_healthy } }` 等待数据库就绪
- 后端服务也配置 `healthcheck`，前端 `depends_on` 后端健康
- 暴露端口：前端 `18702:80`，后端 `${BACKEND_PORT:-19202}:8080`，数据库 `3306:3306`
- 所有可变配置通过环境变量注入，并在 `.env.example` 中给出默认值
- 支持通过 `docker compose up -d` 一条命令启动全部服务

`.env` / `.env.example` 必须包含的键：
- `COMPOSE_PROJECT_NAME=safety-platform`
- 数据库连接相关：`DB_NAME=safety_db`、`DB_USER=safety_user`、`DB_PASSWORD=safety_pwd`、`DB_ROOT_PASSWORD=safety_root`
- 后端：`JWT_SECRET=change_me_to_a_long_random_string`
- 端口映射：`FRONTEND_PORT=18702`、`BACKEND_PORT=19202`、`DB_PORT=3306`

Nginx 反向代理要求（`frontend/nginx.conf`）：
- `location /api/` 反向代理到 `http://backend:8080/`
- 配置 `try_files $uri $uri/ /index.html;` 支持前端 SPA 路由

### 13. Git 要求

代码编写完成后初始化 Git + 创建 `.gitignore`，`.gitignore` 内容按项目技术栈适配：
```
node_modules/
dist/
.env
.DS_Store
*.log
.idea/
.vscode/
backend/server
backend/bin/
*.test
*.out
backend/coverage/
frontend/coverage/
uploads/
```

## README 写法

根目录必须有 `README.md`，用中文说明项目。

README 包含：
- 项目名称和一句话介绍
- 项目主要功能
- 快速启动方式（Docker Compose 一键部署命令）
- 本地开发方式（前端启动命令、后端启动命令）
- 访问地址
- 技术栈
- 项目目录结构
- 环境变量说明
- Docker 部署说明（端口映射、数据卷、常见问题）
- License

README 要求：
- Docker 部署命令放在最前面，作为首选启动方式
- 本地开发方式作为备选
- 命令可直接复制运行
- 目录结构用代码块展示
- 技术栈用表格展示，后端一栏写 `Go 1.22 + Gin + GORM`
- 后端本地开发命令为 `cd backend && go mod tidy && go run ./cmd/server`（或 `go run .`），构建命令为 `go build ./...`
- 不要保留脚手架默认 README 内容
- 必须包含"枚举出现位置清单"，列出 SeverityLevel 和 IncidentStatus 在前后端的所有出现位置

## 自检清单（生成代码后必须确认）

1. 在中文目录名下执行 `docker compose config --quiet` 不报错
2. `docker compose up -d` 后所有容器 healthy
3. 浏览器访问 `http://localhost:18702` 能看到页面，且 `/api/...` 请求返回正常
4. README 中的 Docker 启动命令是 `docker compose up -d`
5. 后端在 `backend/` 下执行 `go mod tidy` 与 `go build ./...` 不报错
6. 核心实体 ≥ 3 个，每个实体明确贯穿全栈
7. 核心页面 ≥ 4 个，至少 2 个页面共用 `<StatusBadge>` 或 `<RiskLevelTag>`
8. 横切关注点 ≥ 2 个，每个写明触达文件层
9. 共享枚举 ≥ 2 组，README 列出所有出现位置
10. 共享前端组件 ≥ 3 个，共享 hooks/utils ≥ 2 个
11. 后端中间件 ≥ 2 个
12. 文件结构强制清单已写入
13. 包含"严禁合并职责到单一文件"
14. 包含屎山代码设计要求
